@layout('main')
@section('content')
    <div class="jumbotron">
        <h1>{{ task.title }}</h1>
        <p class="lead">
            {{ task.body }}
        </p>
        <hr class="my-4">
        <a href="/task" class="btn" style="background-color:#f8f9fa !important">Go back</a>
        <a href="/remove/{{ task.id }}" class="btn btn-danger">Delete</a>
    </div>
@endsection